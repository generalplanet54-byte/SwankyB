# ================================================
# SwankyBoyz.com Environment Variables
# ================================================
# Copy this file to `.env` for local development and fill with your actual credentials.
# DO NOT commit your actual `.env` file with secrets to source control.

# ================================================
# Site URL Configuration
# ================================================
# Optional: Used for generating absolute URLs in meta tags
# Defaults to https://swankyboyz.com if not set
VITE_SITE_URL=https://swankyboyz.com

# ================================================
# Anthropic Claude API Configuration
# ================================================
# Used by Cloudflare Pages Functions for AI-powered article generation
# Generate your API key at: https://console.anthropic.com/
# Model: Claude Haiku (claude-3-5-haiku-20241022)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ================================================
# JWT Secret (Server-Side Authentication)
# ================================================
# Used by Cloudflare Pages Functions for JWT token signing/verification
# CRITICAL: Set this in Cloudflare Pages → Settings → Environment Variables
# Generate a strong random secret (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# ================================================
# Cloudflare Pages Deployment Notes
# ================================================
# 1. Add JWT_SECRET and ANTHROPIC_API_KEY to Cloudflare Pages environment variables:
#    Pages → Settings → Environment Variables → Production/Preview
#
# 2. Never commit sensitive credentials to version control
#
# 3. For local development:
#    - Copy this file to .env
#    - Fill in required values
#    - Run: npm run dev

# ================================================
# Default Admin Credentials (Hardcoded in auth.ts)
# ================================================
# Username: netmin
# Password: P@ssW#rd
# IMPORTANT: Change password immediately after first login using
# the "Change Password" feature in the admin panel
