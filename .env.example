# Copy this file to `.env` for local development and fill with your Supabase credentials.
# Do NOT commit your actual `.env` with secrets to source control.

# Bolt AI Database (primary) - Vite requires the VITE_ prefix to expose at build time
VITE_BOLT_DATABASE_URL=https://wuwczwpfnswwctumvqsq.supabase.co
VITE_BOLT_DATABASE_ANON_KEY=your-bolt-anon-key

# Supabase (fallback) - Vite requires the VITE_ prefix to expose at build time
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: JWT secret used by server-side functions (Pages Functions)
# Set this in Cloudflare Pages Functions environment as JWT_SECRET
JWT_SECRET=your-jwt-secret
