# Cloudflare Pages automatically handles SPA routing
# Only add specific redirects if needed (e.g., legacy URLs)

# API routes should not redirect
/api/* 200

# Static assets should not redirect  
/assets/* 200

# Everything else goes to index.html for React Router (SPA mode)
/* /index.html 200
